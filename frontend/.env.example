VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Set this when frontend and backend are on different domains (e.g. Netlify + Render)
# Leave blank when using Docker/nginx (both on same domain)
VITE_API_URL=
